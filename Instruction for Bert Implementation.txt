I am running the following commands to run the project localy:
# Prepare env
conda create -n dnlp python=3.10
conda activate dnlp
conda install pytorch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 pytorch-cuda=12.1 -c pytorch -c nvidia
conda install tqdm==4.66.2 requests==2.31.0 transformers==4.38.2 tensorboard==2.16.2 tokenizers==0.15.1 -c conda-forge -c huggingface
pip install explainaboard-client==0.1.4 sacrebleu==2.4.0

# Run the script:
# I only modified te last line in run_train.sh could you please check if everything is alright as I have limited knowledge of running jobs on clusters
python multitask_classifier.py --local_files_only --option finetune --task multitask --hidden_dropout_prob 0.1 --epoch 10 --use_gpu
